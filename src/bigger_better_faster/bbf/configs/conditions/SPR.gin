# SPR condition override (load after SPR.gin base config)
# Published SPR agent with dense logging and no augmentation.
# No periodic resets (reset_every defaults to -1). Nature CNN
# encoder, noisy nets enabled, hard target copy every step.
import bigger_better_faster.bbf.agents.metric_agent

BBFAgent.log_every = 100
BBFAgent.data_augmentation = False
