# BBFc condition override (load after BBF.gin base config)
# Lineage control: BBF without the SPR self-predictive system.
# Sets spr_weight=0 and jumps=0 to disable SPR loss and
# transition model rollout. All other BBF parameters inherited.
import bigger_better_faster.bbf.agents.metric_agent

BBFAgent.log_every = 10
BBFAgent.spr_weight = 0
BBFAgent.jumps = 0
BBFAgent.shrink_perturb_keys = "encoder"
