# Beam Rider-specific DQN configuration
# Extends base.yaml with game-specific overrides

defaults:
  - base

experiment:
  name: dqn_beam_rider

env:
  id: "ALE/BeamRider-v5"

training:
  total_frames: 20000000  # 20M frames for Beam Rider
