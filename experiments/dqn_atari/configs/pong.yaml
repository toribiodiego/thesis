# Pong-specific DQN configuration
# Extends base.yaml with game-specific overrides

defaults:
  - base

experiment:
  name: dqn_pong

env:
  id: "ALE/Pong-v5"

training:
  total_frames: 10000000  # 10M frames for Pong
