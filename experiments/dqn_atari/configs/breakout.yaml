# Breakout-specific DQN configuration
# Extends base.yaml with game-specific overrides

defaults:
  - base

experiment:
  name: dqn_breakout

env:
  id: "ALE/Breakout-v5"

training:
  total_frames: 20000000  # 20M frames for Breakout
