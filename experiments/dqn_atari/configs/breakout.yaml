# Breakout-specific DQN configuration
# Extends base.yaml with game-specific overrides

env:
  name: "ALE/Breakout-v5"
  game: "breakout"

# Game-specific overrides (if needed)
# training:
#   frame_budget: 20000000  # 20M frames for Breakout
